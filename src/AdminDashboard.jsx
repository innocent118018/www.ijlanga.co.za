import React,{useEffect,useState}from'react';
import{ArrowLeft,Users,FileText,Package,Search,RefreshCw,CheckCircle2,XCircle,ShoppingBag,Eye}from'lucide-react';
import{supabase}from'./lib/supabase';
import AdminProducts from'./AdminProducts';
const money=n=>`R ${Number(n||0).toLocaleString('en-ZA',{minimumFractionDigits:2})}`;
const date=d=>d?new Date(d).toLocaleString('en-ZA',{dateStyle:'medium',timeStyle:'short'}):'—';
const statuses=['draft','sent','accepted','declined','expired'];
export default function AdminDashboard({onBack}){
 const[tab,setTab]=useState('overview'),[customers,setCustomers]=useState([]),[quotes,setQuotes]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState(''),[notice,setNotice]=useState(''),[search,setSearch]=useState(''),[selected,setSelected]=useState(null),[busy,setBusy]=useState(false);
 useEffect(()=>{load()},[]);
 async function load(){setLoading(true);setError('');const[c, q]=await Promise.all([supabase.from('customers').select('*').order('created_at',{ascending:false}),supabase.from('quotes').select('id,quote_number,customer_id,customer_name,customer_email,status,subtotal,notes,created_at,updated_at,quote_items(id,sku,product_name,quantity,unit_price,price_label)').order('created_at',{ascending:false})]);if(c.error||q.error)setError(c.error?.message||q.error?.message);setCustomers(c.data||[]);setQuotes(q.data||[]);setLoading(false)}
 async function setStatus(q,status){setBusy(true);setError('');const{error:e}=await supabase.from('quotes').update({status,updated_at:new Date().toISOString()}).eq('id',q.id);if(e)setError(e.message);else{setQuotes(xs=>xs.map(x=>x.id===q.id?{...x,status}:x));setNotice(`${q.quote_number} updated to ${status}.`)}setBusy(false)}
 async function convertOrder(q){if(q.status!=='accepted'){setError('Only accepted quotes can be converted to an order.');return}if(!q.quote_items?.length){setError('This quote has no items.');return}setBusy(true);setError('');try{const{data:order,error:e}=await supabase.from('orders').insert({quote_id:q.id,customer_id:q.customer_id,customer_name:q.customer_name,customer_email:q.customer_email,status:'confirmed',payment_status:'unpaid',subtotal:q.subtotal,notes:q.notes||''}).select('id,order_number').single();if(e)throw e;const rows=q.quote_items.map(i=>({order_id:order.id,product_id:null,sku:i.sku,product_name:i.product_name,quantity:i.quantity,unit_price:i.unit_price,price_label:i.price_label}));const{error:itemError}=await supabase.from('order_items').insert(rows);if(itemError)throw itemError;setNotice(`${q.quote_number} converted to ${order.order_number}.`)}catch(e){setError(e.message)}finally{setBusy(false)}}
 const filteredCustomers=customers.filter(c=>`${c.company_name||''} ${c.contact_name} ${c.email} ${c.phone||''}`.toLowerCase().includes(search.toLowerCase()));
 const filteredQuotes=quotes.filter(q=>`${q.quote_number} ${q.customer_name} ${q.customer_email} ${q.status}`.toLowerCase().includes(search.toLowerCase()));
 if(loading)return <div className="admin-shell"><div className="admin-loading">Loading dashboard…</div></div>;
 if(tab==='products')return <AdminProducts onBack={()=>setTab('overview')}/>;
 return <div className="admin-shell"><header className="admin-top"><div><span className="eyebrow">IJ LANGA CONSULTING</span><h1>Administration Dashboard</h1><p>Customers, quotes and service orders in one place.</p></div><div className="admin-top-actions"><button className="admin-btn ghost" onClick={onBack}><ArrowLeft size={16}/> Website</button><button className="admin-btn ghost" onClick={load}><RefreshCw size={16}/> Refresh</button></div></header>
 <nav className="admin-tabs"><button className={tab==='overview'?'active':''} onClick={()=>setTab('overview')}>Overview</button><button className={tab==='customers'?'active':''} onClick={()=>setTab('customers')}><Users size={16}/> Customers</button><button className={tab==='quotes'?'active':''} onClick={()=>setTab('quotes')}><FileText size={16}/> Quotes</button><button className={tab==='products'?'active':''} onClick={()=>setTab('products')}><Package size={16}/> Products</button></nav>
 {notice&&<div className="admin-notice">{notice}</div>}{error&&<div className="admin-error">{error}</div>}
 {tab==='overview'?<><div className="admin-stats"><div><b>{customers.length}</b><span>Customers</span></div><div><b>{quotes.length}</b><span>Total quotes</span></div><div><b>{quotes.filter(q=>q.status==='sent').length}</b><span>Quotes awaiting response</span></div><div><b>{quotes.filter(q=>q.status==='accepted').length}</b><span>Accepted quotes</span></div></div><div className="admin-dashboard-grid"><section className="admin-card"><div className="card-head"><div><span className="eyebrow">LATEST CUSTOMERS</span><h2>New customers</h2></div><button className="admin-btn ghost" onClick={()=>setTab('customers')}>View all</button></div>{customers.slice(0,6).map(c=><div className="admin-list-row" key={c.id}><div><strong>{c.company_name||c.contact_name}</strong><small>{c.contact_name} · {c.email}</small></div><span>{date(c.created_at)}</span></div>)}{!customers.length&&<p className="admin-muted">No customers yet.</p>}</section><section className="admin-card"><div className="card-head"><div><span className="eyebrow">LATEST QUOTES</span><h2>Quote requests</h2></div><button className="admin-btn ghost" onClick={()=>setTab('quotes')}>View all</button></div>{quotes.slice(0,6).map(q=><button className="admin-list-row clickable" key={q.id} onClick={()=>{setSelected(q);setTab('quotes')}}><div><strong>{q.quote_number}</strong><small>{q.customer_name} · {money(q.subtotal)}</small></div><span className={`status-pill ${q.status}`}>{q.status}</span></button>)}{!quotes.length&&<p className="admin-muted">No quotes yet.</p>}</section></div></>:<section className="admin-card"><div className="card-head"><div><span className="eyebrow">{tab==='customers'?'CUSTOMER DIRECTORY':'QUOTE MANAGEMENT'}</span><h2>{tab==='customers'?'Customers':'Quotes'}</h2></div><div className="admin-search"><Search size={16}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder={tab==='customers'?'Search customers…':'Search quotes…'}/></div></div>{tab==='customers'?<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Customer</th><th>Contact</th><th>Email</th><th>Phone</th><th>Joined</th><th>Quotes</th></tr></thead><tbody>{filteredCustomers.map(c=><tr key={c.id}><td><strong>{c.company_name||'Individual'}</strong><small>{c.notes||''}</small></td><td>{c.contact_name}</td><td>{c.email}</td><td>{c.phone||'—'}</td><td>{date(c.created_at)}</td><td>{quotes.filter(q=>q.customer_id===c.auth_user_id).length}</td></tr>)}</tbody></table>{!filteredCustomers.length&&<div className="admin-empty">No customers match your search.</div>}</div>:<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Quote</th><th>Customer</th><th>Amount</th><th>Status</th><th>Created</th><th>Actions</th></tr></thead><tbody>{filteredQuotes.map(q=><tr key={q.id}><td><strong>{q.quote_number}</strong><small>{q.quote_items?.length||0} service line(s)</small></td><td>{q.customer_name}<small>{q.customer_email}</small></td><td>{money(q.subtotal)}</td><td><span className={`status-pill ${q.status}`}>{q.status}</span></td><td>{date(q.created_at)}</td><td><button className="icon-action" title="View quote" onClick={()=>setSelected(q)}><Eye size={16}/></button></td></tr>)}</tbody></table>{!filteredQuotes.length&&<div className="admin-empty">No quotes match your search.</div>}</div>}</section>}
 {selected&&<div className="admin-modal"><div className="admin-form quote-detail"><button className="modal-close" onClick={()=>setSelected(null)}><XCircle/></button><span className="eyebrow">QUOTE DETAIL</span><h2>{selected.quote_number}</h2><p><strong>{selected.customer_name}</strong><br/>{selected.customer_email}</p><div className="quote-detail-items">{selected.quote_items?.map(i=><div className="admin-list-row" key={i.id}><div><strong>{i.product_name}</strong><small>{i.sku} · Qty {i.quantity}</small></div><span>{i.price_label||money(i.unit_price)}</span></div>)}</div><div className="quote-total"><span>Subtotal</span><strong>{money(selected.subtotal)}</strong></div><label>Quote status<select value={selected.status} disabled={busy} onChange={e=>setStatus(selected,e.target.value)}>{statuses.map(s=><option key={s}>{s}</option>)}</select></label>{selected.notes&&<div className="quote-notes"><b>Notes</b><p>{selected.notes}</p></div>}<button className="admin-btn" disabled={busy||selected.status!=='accepted'} onClick={()=>convertOrder(selected)}><ShoppingBag size={16}/> Convert accepted quote to order</button></div></div>}
 </div>;
}
