import React,{useEffect,useState}from'react';
import{createRoot}from'react-dom/client';
import{supabase}from'./lib/supabase';
import AdminDashboard from'./AdminDashboard';
import'./admin.css';
function AdminAuth(){const[session,setSession]=useState(null),[profile,setProfile]=useState(null),[loading,setLoading]=useState(true),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');async function loadSession(s){setSession(s);if(!s){setProfile(null);setLoading(false);return}const{data,error:e}=await supabase.from('profiles').select('id,email,full_name,role').eq('id',s.user.id).maybeSingle();setProfile(data||null);setError(e?.message||'');setLoading(false)}useEffect(()=>{supabase.auth.getSession().then(({data})=>loadSession(data.session));const{data}=supabase.auth.onAuthStateChange((_e,s)=>loadSession(s));return()=>data.subscription.unsubscribe()},[]);async function signIn(e){e.preventDefault();setBusy(true);setError('');const{error:e2}=await supabase.auth.signInWithPassword({email,password});if(e2)setError(e2.message);setBusy(false)}async function signOut(){await supabase.auth.signOut()}if(loading)return <div className="admin-loading">Loading secure admin access…</div>;if(!session)return <main className="admin-auth-page"><form className="admin-auth-card" onSubmit={signIn}><div className="admin-eyebrow">IJ LANGA CONSULTING</div><h1>Secure Admin Portal</h1><p>Sign in with the administrator account configured in Supabase Auth.</p><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email"/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/></label>{error&&<div className="admin-error">{error}</div>}<button disabled={busy}>{busy?'Signing in…':'Sign in'}</button><a href="/">← Return to website</a></form></main>;if(!profile||profile.role!=='admin')return <main className="admin-auth-page"><div className="admin-auth-card"><div className="admin-eyebrow">ACCESS CONTROL</div><h1>Administrator access required</h1><p>This authenticated account does not have the <strong>admin</strong> role.</p>{error&&<div className="admin-error">{error}</div>}<button onClick={signOut}>Sign out</button><a href="/">← Return to website</a></div></main>;return <AdminDashboard onBack={()=>{window.location.href='/'}}/>}
createRoot(document.getElementById('root')).render(<AdminAuth/>);
